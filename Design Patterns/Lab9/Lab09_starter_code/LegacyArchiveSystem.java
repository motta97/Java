public class LegacyArchiveSystem {
    public void store(String title, boolean compress) {
        System.out.println("Legacy archive stored " + title
                + " | compressed=" + compress);
    }
}
